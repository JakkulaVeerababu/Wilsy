{
  "name": "wilsy-directory",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "cd frontend && node scripts/build-site.mjs && node scripts/serve.mjs",
    "build": "cd frontend && node scripts/build-site.mjs",
    "test": "cd frontend && node --test tests/*.test.mjs",
    "build:data": "cd frontend && node scripts/build-data.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  }
}
